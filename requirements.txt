# Vercel（本番）で使う依存。ここに streamlit / pandas は入れない
# （バンドルサイズが上限に近づくため。ローカル UI 用は requirements-dev.txt）
anthropic>=0.100.0
python-dotenv>=1.0
pdfplumber>=0.11
openpyxl>=3.1
python-pptx>=0.6.23
requests>=2.31
beautifulsoup4>=4.12
fastapi>=0.110
python-multipart>=0.0.9
